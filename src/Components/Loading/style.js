import styled from "styled-components";

export const Wrapper = styled.div`
  .wrapper-loading {
    .loading-spinner {
      width: 2rem;
      height: 2rem;
      background-color: var(--indigo);
    }
  }
`;
