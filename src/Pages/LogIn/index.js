import React from "react";
import { LogIn as LogInContent } from "../../Containers";

const LogIn = (props) => {
  return (
    <React.Fragment>
      <LogInContent />
    </React.Fragment>
  );
};

export default LogIn;
