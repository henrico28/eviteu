import React from "react";
import { LogIn } from "../../Containers";

const LogInPage = (props) => {
  return (
    <React.Fragment>
      <LogIn />
    </React.Fragment>
  );
};

export default LogInPage;
