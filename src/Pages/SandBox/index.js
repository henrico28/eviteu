import React from "react";
import { DisplayLocation } from "../../Components";

const SandBox = (props) => {
  return (
    <React.Fragment>
      <DisplayLocation />
    </React.Fragment>
  );
};

export default SandBox;
