import React from "react";
import { Error } from "../../Containers";

const SandBox = (props) => {
  return (
    <React.Fragment>
      <Error />
    </React.Fragment>
  );
};

export default SandBox;
