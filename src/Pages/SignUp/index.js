import React from "react";
import { SignUp as SignUpContent } from "../../Containers";

const SignUp = (props) => {
  return (
    <React.Fragment>
      <SignUpContent />
    </React.Fragment>
  );
};

export default SignUp;
