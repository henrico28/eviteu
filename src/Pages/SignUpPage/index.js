import React from "react";
import { SignUp } from "../../Containers";

const SignUpPage = (props) => {
  return (
    <React.Fragment>
      <SignUp />
    </React.Fragment>
  );
};

export default SignUpPage;
