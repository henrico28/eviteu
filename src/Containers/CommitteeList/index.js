import React from "react";
import { Wrapper } from "./style";

const CommitteeList = (props) => {
  return (
    <Wrapper>
      <div>CommitteeList</div>
    </Wrapper>
  );
};

export default CommitteeList;
