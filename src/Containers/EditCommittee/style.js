import styled from "styled-components";

export const Wrapper = styled.div`
  .wrapper-edit-committee {
    min-height: 85vh;
    margin: 10px 8px 8p 8px;
    background-color: var(--white);

    .edit-committee-title {
      font-size: 16px;
    }
  }
`;
