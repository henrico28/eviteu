import styled from "styled-components";

export const Wrapper = styled.div`
  .wrapper-edit-announcement {
    min-height: 85vh;
    margin: 10px 8px 8p 8px;
    background-color: var(--white);

    .edit-announcement-title {
      font-size: 16px;
    }
  }
`;
