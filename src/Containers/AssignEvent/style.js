import styled from "styled-components";

export const Wrapper = styled.div`
  .wrapper-assign-event {
    min-height: 85vh;
    margin: 10px 8px 8px 8px;
    background-color: var(--white);

    .assign-event-title {
      font-size: 16px;
    }
  }
`;
