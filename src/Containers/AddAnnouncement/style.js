import styled from "styled-components";

export const Wrapper = styled.div`
  .wrapper-add-announcement {
    min-height: 85vh;
    margin: 10px 8px 8px 8px;
    background-color: var(--white);

    .add-announcement-title {
      font-size: 16px;
    }
  }
`;
