import styled from "styled-components";

export const Wrapper = styled.div`
  .wrapper-assign-committee {
    min-height: 85vh;
    margin: 10px 8px 8px 8px;
    background-color: var(--white);

    .assign-committee-title {
      font-size: 16px;
    }
  }
`;
