import styled from "styled-components";

export const Wrapper = styled.div`
  .wrapper-doorprize-list {
    min-height: 85vh;
    margin: 10px 8px 8px 8px;
    background-color: var(--white);

    .doorprize-list-title {
      font-size: 16px;
    }

    .wrapper-doorprize-list-search {
      display: flex;
      justify-content: flex-end;
      .doorprize-list-search-input {
        width: 60%;
      }
    }
  }
`;
